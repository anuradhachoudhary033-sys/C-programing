#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    float c,m,km;
    scanf("%f",&c);
    m=c/100;
    km=m/1000;
    printf("%.2f %.2f",m,km);
    return 0;
}
